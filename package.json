{
  "name": "blog",
  "version": "0.0.1",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "cross-env NODE_ENV=production node index.js",
    "dev": "cross-env NODE_ENV=development nodemon index.js",
    "test": "cross-env NODE_ENV=test node --test",
    "test:e2e": "cross-env NODE_ENV=test playwright test --project=chromium",
    "build:ui": "rm -rf dist && cd ./frontend && npm run build && cp -r dist ../",
    "deploy": "fly deploy",
    "deploy:full": "npm run build:ui && npm run deploy",
    "logs:prod": "fly logs",
    "lint": "eslint .",
    "start:test": "cross-env NODE_ENV=test node index.js"
  },
  "author": "David Zhang",
  "license": "MIT",
  "dependencies": {
    "bcrypt": "^5.1.1",
    "cors": "^2.8.5",
    "cross-env": "^7.0.3",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "express-async-errors": "^3.1.1",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^8.3.2",
    "n_": "^4.0.1",
    "vite": "^5.2.11"
  },
  "devDependencies": {
    "@eslint/js": "^9.2.0",
    "@flydotio/dockerfile": "^0.5.5",
    "@playwright/test": "^1.56.1",
    "@stylistic/eslint-plugin-js": "^2.1.0",
    "@types/node": "^20.14.2",
    "eslint": "^9.2.0",
    "globals": "^15.2.0",
    "nodemon": "^3.1.0",
    "supertest": "^7.0.0"
  }
}
